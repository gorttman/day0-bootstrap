apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dns-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "50"
spec:
  project: default
  source:
    repoURL: https://github.com/gorttman/dns-conf.git
    targetRevision: main
    path: pihole
  destination:
    server: https://kubernetes.default.svc
    namespace: pihole
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
